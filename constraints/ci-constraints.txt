# Pinned constraints for CI to avoid resolver backtracking and version mismatches
# These constraints are applied on top of requirements.txt during CI installs.

# HF stack (compatible pairing)
transformers==4.55.2
tokenizers==0.21.4

# SQLAlchemy/greenlet stability
greenlet==3.2.4

# contourpy compatibility per Python version
contourpy==1.3.2; python_version < "3.11"
contourpy==1.3.3; python_version >= "3.11"

